{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "prestart": "cd ../../.. && pnpm doc:copy:changelog && pnpm check:doc-inline-types && cd packages/main/website && pnpm run generate:images && pnpm run generate:emojis && pnpm run generate:bundle-data && pnpm run generate:comparisons && pnpm run generate:use-cases && pnpm run generate:embeddings && pnpm run build:demo",
    "start": "docusaurus start",
    "prebuild": "cd ../../../ && pnpm build && cd packages/main/website && pnpm run generate:images && pnpm run generate:emojis && pnpm run generate:docs && pnpm run generate:bundle-data && pnpm run generate:comparisons && pnpm run generate:embeddings && pnpm run build:demo",
    "generate:docs": "cd ../.. && pnpm doc:generate",
    "generate:use-cases": "pnpm exec tsx scripts/generate-use-cases-data.ts",
    "generate:embeddings": "pnpm exec tsx scripts/generate-embeddings.ts",
    "build": "docusaurus build",
    "build:demo": "pnpm exec tsx scripts/build-demo.ts",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids",
    "typecheck": "tsc",
    "generate:images": "pnpm exec tsx scripts/generate-images.ts",
    "generate:emojis": "pnpm exec tsx scripts/generate-emojis.ts",
    "generate:fonts": "pnpm exec tsx scripts/generate-fonts.ts",
    "generate:bundle-data": "pnpm exec tsx scripts/generate-bundle-data.ts",
    "generate:comparisons": "pnpm exec tsx scripts/generate-precomputed-comparisons.ts"
  },
  "dependencies": {
    "@docusaurus/core": "3.9.2",
    "@docusaurus/plugin-content-docs": "3.9.2",
    "@docusaurus/preset-classic": "3.9.2",
    "@docusaurus/theme-common": "3.9.2",
    "@docusaurus/theme-mermaid": "^3.9.2",
    "@mdx-js/react": "^3.0.0",
    "@tanstack/react-virtual": "^3.13.18",
    "@xenova/transformers": "^2.17.2",
    "clsx": "^2.0.0",
    "pithos": "workspace:*",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.9.2",
    "@docusaurus/tsconfig": "3.9.2",
    "@docusaurus/types": "3.9.2",
    "mdast-util-to-string": "^4.0.0",
    "remark-parse": "^11.0.0",
    "typescript": "~5.6.2",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  }
}
