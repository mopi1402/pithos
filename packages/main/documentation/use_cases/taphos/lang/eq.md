## `eq`

### **Compare** values 📍

@keywords: equal, compare, strict, equality

Strict equality comparison.

```typescript
a === b;
```

### **Check** identity

@keywords: identity, same, reference

Check if two values are identical.

```typescript
const obj = { id: 1 };
const ref = obj;
obj === ref;  // true
```
