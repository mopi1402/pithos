## `lt`

### **Compare** less than 📍

@keywords: less, compare, number, order

Check if first value is less than second.

```typescript
a < b;
```

### **Check** limit

@keywords: limit, maximum, validation

Validate value is under limit.

```typescript
if (items.length < maxItems) {
  items.push(newItem);
}
```
