## `startsWith`

### **Check** prefix 📍

@keywords: starts, prefix, check, string

Check if string starts with value.

```typescript
'hello world'.startsWith('hello');  // true
```

### **Validate** protocol

@keywords: validate, protocol, url, prefix

Check URL protocol.

```typescript
const isSecure = url.startsWith('https://');
```
