## `startsWith` / `endsWith`

### **Check** string prefix 📍

@keywords: starts, ends, prefix, suffix, check

Check if string starts or ends with a value.

```typescript
"hello world".startsWith("hello");  // => true
"hello world".endsWith("world");    // => true
```

### **Validate** file extension

@keywords: validate, extension, file, type

Check file type by extension.

```typescript
filename.endsWith(".json");
filename.endsWith(".ts");
```

### **Check** URL protocol

@keywords: check, URL, protocol, https

Validate URL protocol.

```typescript
url.startsWith("https://");
url.startsWith("http://");
```
