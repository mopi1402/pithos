## `upperCase`

### **Convert** to upper case words 📍

@keywords: upper, case, words, convert

Convert string to space-separated uppercase words.

```typescript
'camelCase'.toUpperCase().replace(/([A-Z])/g, ' $1').trim();
```

### **Format** header

@keywords: format, header, title, display

Format text for headers.

```typescript
const header = text.toUpperCase();
```
