## `repeat`

### **Repeat** string 📍

@keywords: repeat, duplicate, multiply, string

Repeat string n times.

```typescript
"ab".repeat(3);  // => "ababab"
"-".repeat(10);  // => "----------"
```

### **Create** separator

@keywords: separator, line, divider

Build visual separators.

```typescript
const divider = "=".repeat(40);
console.log(divider);
```

### **Indent** code

@keywords: indent, spaces, format

Create indentation.

```typescript
const indent = "  ".repeat(level);
const line = indent + content;
```
