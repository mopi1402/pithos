## `lowerCase`

### **Convert** to lower case words 📍

@keywords: lower, case, words, convert

Convert string to space-separated lowercase words.

```typescript
'camelCase'.toLowerCase().replace(/([A-Z])/g, ' $1').trim();
// Or use kebabCase/snakeCase from arkhe
```

### **Normalize** text

@keywords: normalize, text, display, format

Format text for display.

```typescript
const display = text.toLowerCase();
```
