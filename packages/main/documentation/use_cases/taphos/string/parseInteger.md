## `parseInteger`

### **Parse** integer 📍

@keywords: parse, integer, number, string

Parse string to integer.

```typescript
parseInt('42', 10);  // 42
```

### **Convert** user input

@keywords: convert, input, form, number

Parse form input to number.

```typescript
const quantity = parseInt(input, 10) || 0;
```
