## `join`

### **Create** comma-separated string 📍

@keywords: join, comma, string, list

Join array elements into a string.

```typescript
const tags = ["javascript", "typescript", "nodejs"];
tags.join(", ");
// => "javascript, typescript, nodejs"
```

### **Build** path from segments

@keywords: build, path, segments, URL

Combine path segments with a separator.

```typescript
const segments = ["home", "user", "documents"];
segments.join("/");
// => "home/user/documents"
```

### **Format** display list

@keywords: format, display, list, readable

Create readable lists for display.

```typescript
const names = ["Alice", "Bob", "Charlie"];
names.join(" and ");
// => "Alice and Bob and Charlie"
```
