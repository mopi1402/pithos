## `last`

### **Get** last element 📍

@keywords: last, element, final, array

Retrieve the last element of an array.

```typescript
const items = [1, 2, 3, 4, 5];
items.at(-1);
// => 5
```

### **Get** most recent item

@keywords: recent, latest, history, stack

Get the most recent entry from a history.

```typescript
const history = ["page1", "page2", "page3"];
history.at(-1);
// => "page3"
```

### **Access** final result

@keywords: access, final, result, chain

Get the last item from a processed array.

```typescript
const results = computeAll();
results.at(-1);
// => final result
```
