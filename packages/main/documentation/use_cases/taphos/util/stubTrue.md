## `stubTrue`

### **Return** true 📍

@keywords: stub, true, boolean, default

Return true as default.

```typescript
const alwaysTrue = () => true;
```

### **Enable** by default

@keywords: enable, default, feature, flag

Default to enabled state.

```typescript
const isEnabled = config.feature ?? true;
```
