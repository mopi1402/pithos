## `max`

### **Find** maximum 📍

@keywords: max, maximum, largest, highest

Find largest value.

```typescript
Math.max(1, 5, 3);     // 5
Math.max(...numbers);  // from array
```

### **Clamp** minimum

@keywords: clamp, minimum, floor, bound

Ensure value is at least minimum.

```typescript
const atLeast = Math.max(value, minimum);
```
