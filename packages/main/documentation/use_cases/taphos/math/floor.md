## `floor`

### **Round** down 📍

@keywords: floor, round, down, truncate

Round number down to nearest integer.

```typescript
Math.floor(4.9);   // 4
Math.floor(-4.1);  // -5
```

### **Calculate** index

@keywords: index, array, position

Calculate array index from ratio.

```typescript
const index = Math.floor(Math.random() * items.length);
```
