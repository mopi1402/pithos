## `add`

### **Add** numbers 📍

@keywords: add, sum, plus, arithmetic

Add two numbers.

```typescript
a + b;
```

### **Accumulate** values

@keywords: accumulate, reduce, sum, total

Sum array of numbers.

```typescript
const total = numbers.reduce((a, b) => a + b, 0);
```
