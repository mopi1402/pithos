## `ceil`

### **Round** up 📍

@keywords: ceil, round, up, ceiling

Round number up to nearest integer.

```typescript
Math.ceil(4.1);   // 5
Math.ceil(-4.9);  // -4
```

### **Calculate** pages

@keywords: pages, pagination, ceiling

Calculate total pages needed.

```typescript
const totalPages = Math.ceil(items.length / pageSize);
```
