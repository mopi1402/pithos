## `min`

### **Find** minimum 📍

@keywords: min, minimum, smallest, lowest

Find smallest value.

```typescript
Math.min(1, 5, 3);     // 1
Math.min(...numbers);  // from array
```

### **Clamp** maximum

@keywords: clamp, maximum, ceiling, bound

Ensure value is at most maximum.

```typescript
const atMost = Math.min(value, maximum);
```
