## `isSet`

### **Identify** unique collections

@keywords: identify, Set, unique, collections, deduplication, membership

Check if a value is a Set instance.
Useful when working with collections that require uniqueness.

```typescript
if (isSet(items)) {
  console.log(items.has(value));
}
```
