## `isSymbol`

### **Check** for symbols

@keywords: check, Symbol, unique, keys, private, properties

Verify if a value is a Symbol.
Useful for checking object property keys that are meant to be private/unique.

```typescript
if (isSymbol(key)) {
  // Handle symbol key
}
```
