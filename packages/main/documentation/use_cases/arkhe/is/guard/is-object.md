## `isObject`

### **Check** for object types

@keywords: check, object, types, generic, validation, narrowing

Generic check for any object type (including arrays, dates, etc.).
Useful as a broad check before specific type narrowing.

```typescript
if (isObject(value)) {
  // value is object type
}
```
