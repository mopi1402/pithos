{
  "name": "@pithos/inlining-test",
  "version": "1.0.0",
  "description": "Test d'inlining automatique avec tsup + Terser",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "build:dev": "tsup --config tsup.config.dev.ts",
    "test": "node dist/index.js"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
